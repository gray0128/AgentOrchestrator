# 评估差异任务清单

生成时间：2026-06-27；复核修订：2026-06-27；GitHub 录入：2026-06-27

## 用途

本文档把 `docs/评估报告/` 下四份一致性评估报告中的差异项重新去重、复核并整理为后续 GitHub issue 录入清单。

本文档不是实时任务看板。录入 issue 后，任务状态、优先级、讨论、验收和关闭记录以 GitHub milestones / issues / PRs 为准。

## 输入来源

- `docs/评估报告/claudecode/方案-合同-代码一致性评估报告.md`
- `docs/评估报告/codex/文档与代码一致性评估报告.md`
- `docs/评估报告/grok/文档与代码一致性评估报告.md`
- `docs/评估报告/antigravity/方案-合同-代码一致性评估报告.md`

## 复核依据

- 主方案：`github-native-agent-orchestrator-自动处理-issue-方案.md`
- 契约：`docs/contracts/`
- API 设计：`docs/api-design/`
- 开发计划：`docs/development-plan/`
- 当前实现：`src/`、`test/`
- 当前 GitHub issue 状态：截至 2026-06-27，M8/M9 主要硬化任务已关闭；M10-M12 仍有 open issue。

## 复核结论

四份报告对总体判断一致：核心 MVP 已可运行，状态机、SQLite、agent adapter、GitHub 写适配器、check 聚合、reconciliation、local UI 与多数契约已经对齐。后续差异不应再按“早期 MVP 缺口”整体打包，而应拆成三类：

1. 文档漂移：主方案仍保留早期 YAML、无 UI、无 Triage、旧 webhook 清单等描述。
2. 实现缺口：Webhook 事件覆盖、merge gate 当前 PR 证据、material GitHub write 的幂等动作审计、scheduler label-gating、状态 label 同步、控制命令、安全 enforcement 等仍未闭合。
3. 契约/Schema 小不一致：marker verdict、DomainEvent 类型、PR review 模板、Required Schemas 清单等需要收敛。

## 不再作为新任务录入的差异

以下差异已由现有 issue 覆盖或复核后不成立，不建议重复录入：

| 差异 | 复核结论 |
| --- | --- |
| workspace manager 未接入 full lifecycle | 已由 GitHub issue #3 关闭。 |
| full lifecycle 未按实际 git diff 执行 path policy | 已由 GitHub issue #4 关闭。 |
| fake file content / fake base sha 兜底 | 已由 GitHub issue #5 关闭。 |
| runtime lifecycle 裸 `Error` 过多 | 已由 GitHub issue #6 关闭。 |
| webhook delivery 去重未持久化 | 已由 GitHub issue #7 关闭。 |
| agent env blacklist 不安全 | 已由 GitHub issue #8 关闭。 |
| resume context 仍是 stub | 已由 GitHub issue #9 关闭。 |
| PR review / CI fix loop 不完整 | 已由 GitHub issue #10、#11 关闭。 |
| minimal scheduler 缺失 | 已由 GitHub issue #13 关闭。 |
| crash-recovery / idempotent action 测试已闭合全部写审计 | 不成立。已关闭的 #12 仅补齐 recovery 测试与 reconciliation backfill，不等同于 full lifecycle 中 branch/commit/PR/review/merge/close 等 material write 的完整 `idempotent_actions` 审计；缺口保留在 AO-EVAL-05。 |
| `idempotent_actions.target_type` 可空 | 不成立。当前 SQLite DDL 为 `target_type TEXT NOT NULL`，与 `data-contracts.md` 一致。 |
| high-risk path 不会写 `needs-human` / `agent:blocked` | 复核为已实现主要副作用：`buildBlockedHandling()` 使用 `syncStateLabels()`，blocked 路径会写 blocked comment 和 labels。仍可由具体测试 issue 补强，但不作为独立缺口。 |
| legacy commit status 完全未读取 | 不准确。REST adapter 的 `readCheckSummary()` 已读取 combined commit status；缺口是 `status` webhook 未归一化。 |
| `PrOpened` / `PrReviewing` / `CiWaiting` 必须拆成不同 label | 不成立。主方案和当前实现均将这些状态合并为 `agent:pr-review`；如要拆分应先做产品决策。 |

## 与现有 open issue 交叉引用

录入新 issue 前先核对下表，避免重复或碎片化。已有 open issue 优先跟进，不重复新建。

| 清单项 | 已有 open issue | 关系 |
| --- | --- | --- |
| AO-EVAL-01 | #19、#21、#23 | 文档类任务可能拆分；录入时注明与主方案对齐的边界，避免与 copyable policy / readiness / global install 文档重复 |
| AO-EVAL-06 | #41 | 重叠：CI webhook resume 需保留 live issue labels；#41 聚焦 resume 路径，AO-EVAL-06 聚焦主生命周期状态 label 同步 |
| AO-EVAL-08、AO-EVAL-09 | #17 | 验证重叠：redaction / marker parsing 定点测试可在实现 issue 中引用 #17，不必重复建测试 issue |
| AO-EVAL-13 | #40 | **已存在**，无需重复录入；清单条目仅作对齐，直接跟进 #40 |
| — | #14–#18 | M10 测试矩阵已规划；新硬化/契约 issue 的验证步骤可引用这些 issue，不重复建 smoke/coverage matrix |

## 已录入 GitHub issue

| 清单项 | GitHub issue | 状态 |
| --- | --- | --- |
| AO-EVAL-01 | [#50](https://github.com/gray0128/AgentOrchestrator/issues/50) | open |
| AO-EVAL-02 | [#44](https://github.com/gray0128/AgentOrchestrator/issues/44) | open |
| AO-EVAL-03 | [#45](https://github.com/gray0128/AgentOrchestrator/issues/45) | open |
| AO-EVAL-04 | [#42](https://github.com/gray0128/AgentOrchestrator/issues/42) | open |
| AO-EVAL-05 | [#43](https://github.com/gray0128/AgentOrchestrator/issues/43) | open |
| AO-EVAL-06 | [#46](https://github.com/gray0128/AgentOrchestrator/issues/46) | open |
| AO-EVAL-07 | [#49](https://github.com/gray0128/AgentOrchestrator/issues/49) | open |
| AO-EVAL-08 | [#48](https://github.com/gray0128/AgentOrchestrator/issues/48) | open |
| AO-EVAL-09 | [#47](https://github.com/gray0128/AgentOrchestrator/issues/47) | open |
| AO-EVAL-10 | [#51](https://github.com/gray0128/AgentOrchestrator/issues/51) | open |
| AO-EVAL-11 | [#52](https://github.com/gray0128/AgentOrchestrator/issues/52) | open |
| AO-EVAL-12 | [#53](https://github.com/gray0128/AgentOrchestrator/issues/53) | open |
| AO-EVAL-13 | [#40](https://github.com/gray0128/AgentOrchestrator/issues/40) | open（录入前已存在） |

## 待录入任务

### AO-EVAL-01：同步主方案与当前 MVP 事实

建议标题：`Align main architecture plan with current MVP contracts and runtime`

优先级：P1

建议标签：`type:docs`、`area:readiness`、`area:contracts`、`priority:p1`

建议 milestone：M11 - Operator Readiness

关联 issue：#19、#21、#23（录入时注明边界，避免重复）

背景：

- 主方案仍写 `.github/agent-orchestrator.yml`、`~/.agent-orchestrator/config.yml`、不做本地 Web UI。
- 当前契约和实现使用 `.github/agent-orchestrator.json`、`config/local.json`，且已经实现 `ao ui` 只读本地 operator UI。
- 主方案未把 `triage` 作为一等分发阶段，也未明确 `merge_agent` 是 deterministic marker role，而不是普通 agent adapter role。

范围：

- 更新主方案的 MVP 边界：不做完整用户控制台，但保留 `ao ui` 只读 operator UI。
- 将仓库策略示例改为 `.github/agent-orchestrator.json`。
- 将本地配置说明对齐 `local-config.schema.json`。
- 补充 Triage 阶段、mention dispatch、`dispatch` envelope 字段。
- 将 `merge_agent` 明确为 deterministic merge gate / marker role。
- 更新过期 SQLite 示例，使用 `last_error_code` / `last_error_message` 和当前四张表。

验收标准：

- 主方案不再与 `docs/contracts/`、`docs/api-design/` 的当前 MVP 事实冲突。
- 新读者能从主方案直接理解 Triage、local UI、JSON 配置和 merge gate 角色边界。
- 文档中保留“GitHub 是用户可见事实来源，SQLite 是调度缓存”的定位。

验证：

- `npm run check`
- 人工核对主方案中的 `.yml`、`config.yml`、`不做本地 Web UI`、`Merge Agent` 旧语义已处理。

### AO-EVAL-02：统一 Webhook intake API 路径和响应语义

建议标题：`Align webhook intake path and response status contract`

优先级：P1

建议标签：`type:bug`、`area:webhook`、`area:api`、`priority:p1`

建议 milestone：M10 - Quality Gates And Verification

背景：

- `docs/api-design/04-management-apis.md` 定义 `POST /webhooks/github`，但 `ao serve` 实现和 `ao live-smoke` 使用 `POST /webhook`。
- API 文档要求 invalid signature 返回 401、duplicate delivery 返回 202；当前实现中签名异常统一返回 400，duplicate delivery 返回 200。

范围：

- 决定 canonical webhook path：保留 `/webhook` 并改文档，或代码兼容 `/webhooks/github`。
- 对齐 invalid signature、unsupported event、duplicate delivery 的 HTTP status 和 delivery status。
- 更新 `ao live-smoke` 文档或行为。
- 补充 CLI/webhook tests。

验收标准：

- `04-management-apis.md`、`05-cli-commands.md`、README、`startServeRuntime()` 路径一致。
- invalid signature、duplicate delivery 的测试覆盖文档承诺。
- duplicate delivery 不重复推进 workflow。

验证：

- `npm run check`
- 定点运行 webhook/CLI 相关测试。

### AO-EVAL-03：补齐或显式裁剪 Webhook 事件覆盖

建议标题：`Close webhook event coverage gaps against the main plan`

优先级：P1

建议标签：`type:feature`、`area:webhook`、`area:runtime`、`priority:p1`

建议 milestone：M10 - Quality Gates And Verification

背景：

- 主方案列出 `issues.opened`、`pull_request.opened/reopened`、`pull_request_review.submitted`、`status`、`issues.unlabeled` 等事件。
- 当前 `normalizeGitHubWebhook()` 仅覆盖 `issues.labeled/unlabeled(agent:pause)`、mention dispatch、`pull_request.synchronize`、`check_run`、`workflow_run`，并扩展支持 `pull_request_review_comment.created` mention。

范围：

- 决定每类事件是实现、降维替代，还是明确不在 MVP 直接处理。
- 优先补齐 `pull_request_review.submitted`、`status`、`issues.opened` with `agent:autopilot`、`issues.unlabeled` removing `agent:autopilot`。
- 明确 `pull_request.opened/reopened` 是直接 domain event，还是仅由 mention / reconciliation 驱动。
- 更新主方案、domain-event schema、tests。

验收标准：

- 主方案 webhook 清单与 `DomainEventType` / `normalizeGitHubWebhook()` 的真实支持面一致。
- 对实现的事件有 domain-event tests。
- 对裁剪的事件有文档说明和替代机制。

验证：

- `npm run check`
- `test/domain-event.test.ts` 覆盖新增或裁剪后的事件语义。

### AO-EVAL-04：收敛 Query API 与 merge gate 当前 PR 证据

建议标题：`Wire current GitHub PR context into merge gate prechecks`

优先级：P0

建议标签：`type:hardening`、`area:api`、`area:runtime`、`area:policy`、`priority:p0`

建议 milestone：M10 - Quality Gates And Verification（M8 已关闭，P0 硬化项归入 M10）

背景：

- `docs/api-design/02-query-apis.md` 定义 `getIssueContext`、`getPullRequestContext` 和 `PolicyEngine.evaluateMergeGate`。
- 当前 `GitHubApiAdapter` 没有同名 query 方法；相关能力分散在 lifecycle、artifact reader 和 `readCheckSummary()`。
- `finishMergeAndCloseout()` 调用 `evaluateMergeGate()` 时传入 `githubMergeable: true`，没有在 merge gate 前读取 PR `mergeable` / `mergeable_state`、ruleset 或 branch protection evidence。

范围：

- 增加或明确 query adapter 边界，至少能读取当前 PR head、mergeability、reviews、checks/statuses、labels。
- 将 merge gate 输入改为当前 GitHub 事实，而不是硬编码 `githubMergeable: true`。
- 明确 `branch_protection_read` 的实现、降级或禁用语义。
- 对 GitHub 正在计算 mergeability 的状态返回 `WAIT`。

验收标准：

- merge gate 前会重新读取当前 PR context。
- stale head、mergeability pending、mergeability blocked、required checks/statuses failure 都有确定性决策。
- 最终 merge 仍通过 GitHub Merge API 并携带 expected head sha。

验证：

- `npm run check`
- 新增 merge gate / REST adapter / lifecycle tests 覆盖 `githubMergeable=false`、mergeability pending、branch protection source 行为。

### AO-EVAL-05：补齐 full lifecycle 的 `idempotent_actions` 审计记录

建议标题：`Record idempotent action rows for every GitHub write in lifecycle`

优先级：P0

建议标签：`type:hardening`、`area:idempotency`、`area:runtime`、`priority:p0`

建议 milestone：M10 - Quality Gates And Verification（M8/M9 已关闭，P0 硬化项归入 M10）

背景：

- GitHub write adapter 方法都接收 `idempotencyKey` 和 `requestHash`。
- `recordCompletedAction()` 已覆盖 plan / plan-review / fix 评论、blocked 路径的评论与 label、`read_check_summary`；webhook-runtime 与 issue-dispatch 也有部分记录。
- 仍未写入 `idempotent_actions` 的 material writes 包括：`createBranch`、`commitChanges`、`createOrUpdatePullRequest`、`submitPullRequestReview`、`mergePullRequest`、`deleteBranch`、`closeIssue`、closeout 最终 summary 评论。
- 已关闭的 #12 补齐了 crash-recovery 测试与 reconciliation backfill，不等于上述 material write 审计已闭合。

范围：

- 增加统一 GitHub write executor 或在生命周期各写点记录 `pending -> completed/failed/skipped`。
- 补齐尚未记录的 material writes：branch、commit、PR、PR review、merge、delete branch、final summary、close issue；与已有 comment/label 记录统一格式。
- response_ref 使用 GitHub adapter 返回值或可恢复 artifact identity。
- 明确 crash 后远端成功、本地未记录时的 reconciliation backfill 关系。

验收标准：

- 每个 material GitHub write 都有 `idempotent_actions` row。
- replay 使用相同 key 不重复写远端。
- request hash 冲突会阻断或报错。
- UI run detail 能看到完整 action audit。

验证：

- `npm run check`
- 新增 full lifecycle action audit tests。
- 新增 crash/replay 测试覆盖远端成功、本地 action 记录缺失的恢复路径。

### AO-EVAL-06：将状态 label 同步接入主生命周期状态转移

建议标题：`Synchronize GitHub state labels on visible workflow transitions`

优先级：P1

建议标签：`type:hardening`、`area:runtime`、`area:state`、`priority:p1`

建议 milestone：M10 - Quality Gates And Verification

关联 issue：#41（CI resume live labels，重叠但不等价）

背景：

- `syncStateLabels()` 已实现并有测试，但主要由 blocked handling 使用。
- 普通状态推进不会在每个用户可见阶段同步 `agent:planning`、`agent:plan-review`、`agent:implementing`、`agent:pr-review`、`agent:merge-ready`、`agent:done`。
- open issue #41 处理 CI webhook resume 保留 live labels，与本任务有重叠但不完全等价。

范围：

- 明确哪些 workflow transitions 需要写 GitHub labels。
- 在状态 transition side effect 中调用 `syncStateLabels()`，保持状态 label 互斥并保留类型/风险/控制 labels。
- 将 label writes 纳入 `idempotent_actions`。
- 避免 CI webhook resume 使用旧 issue snapshot 覆盖 live labels。

验收标准：

- 主生命周期用户可见状态与 GitHub labels 一致。
- 控制 labels、风险 labels、类型 labels 不被误删。
- `agent:done` 在 merged / issue_closed closeout 后可见。

验证：

- `npm run check`
- full lifecycle label sync integration tests。

### AO-EVAL-07：实现或重新定义 Issue 评论控制命令

建议标题：`Implement or de-scope slash control commands`

优先级：P1

建议标签：`type:feature`、`area:webhook`、`area:policy`、`priority:p1`

建议 milestone：M10 - Quality Gates And Verification

背景：

- `04-management-apis.md` 定义 `/agent pause|resume|retry|no-merge|use ...`。
- 当前实现主要支持 `agent:pause`、`agent:no-merge` labels 和 mention dispatch；没有完整解析 slash control commands。
- 状态机 guard 文案提到 `/agent pause`，实现语义尚未闭合。

范围：

- 决定 slash commands 是 MVP 必需能力，还是仅保留 label 控制。
- 如实现：解析 allowed actors 的 Issue comments，转换为 domain events 或明确 workflow actions。
- 支持 pause、resume、retry、no-merge；`use implementer/reviewer` 可作为后续任务。
- 如裁剪：更新 API 设计和状态机 guard 文案。

验收标准：

- 文档与实现不再冲突。
- 控制命令不能绕过 policy、CI、review、ruleset、high-risk blocks。
- 不允许未授权 actor 控制 workflow。

验证：

- `npm run check`
- 新增 command parsing、actor gating、state transition tests。

### AO-EVAL-08：补齐 prompt injection policy enforcement

建议标题：`Enforce prompt-injection policy violations in runtime gates`

优先级：P1

建议标签：`type:security`、`area:security`、`area:policy`、`priority:p1`

建议 milestone：M10 - Quality Gates And Verification

关联 issue：#17（验证可引用 redaction / marker parsing 测试）

背景：

- `PROMPT_INJECTION_POLICY_VIOLATION` 错误码已注册。
- `security-and-audit-contracts.md` 要求影响执行的 prompt injection violation 进入 blocked。
- 当前未看到明确检测和阻断路径。

范围：

- 先定义 MVP 可执行检测范围，不做泛化内容审核。
- 最小覆盖：请求泄露 token/secret、跳过 gate、扩大 write path、改变 agent identity/network、把 high-risk 标成 low risk。
- 在 issue/comment/PR body/agent output 进入执行前的关键 gate 加检测。
- 命中后 blocked comment、`needs-human`、`agent:blocked`、last_error_code 一致。

验收标准：

- 明确的策略绕过文本不会进入 agent 或 GitHub write 阶段。
- 错误码和 blocked artifact 可审计。
- 低误报策略在文档中说明。

验证：

- `npm run check`
- 新增 security/policy tests 覆盖每类最小检测。

### AO-EVAL-09：修复 Schema、marker 和 artifact template 不一致

建议标题：`Align marker schemas and artifact templates with rendered output`

优先级：P1

建议标签：`type:bug`、`area:contracts`、`area:security`、`priority:p1`

建议 milestone：M10 - Quality Gates And Verification

关联 issue：#17（验证可引用 marker parsing 测试）

背景：

- `renderFixComment()` 输出 marker verdict `FIX_READY`，但 `agent-marker.schema.json` 的 verdict enum 不包含 `FIX_READY`。
- `github-artifact-templates.md` 的 PR review 模板标题是 `## Agent PR Review`，并要求 `## Blocking Findings`；当前 `renderPrReviewComment()` 输出 `## PR Review` 且缺少 Blocking Findings 段。
- `contracts/README.md` Required Schemas 未列出 `triage-result.schema.json`，但该 schema 已存在并已用于运行时校验。
- `action-proposal.schema.json` 被列为 Required，但运行时未消费，应标为预留或落地使用。

范围：

- 修复 marker schema 与实际 render 输出的一致性。
- 修复 PR review artifact template 或 render 代码，使二者一致。
- 更新 `contracts/README.md` Required Schemas。
- 明确 `action-proposal` 是 reserved 还是 required runtime contract。

验收标准：

- 当前渲染的 planner / plan review / fix / PR review / final summary marker 能通过 schema。
- PR review comment 包含约定标题和 Blocking Findings。
- Required Schemas 清单反映真实运行时依赖。

验证：

- `npm run check`
- 新增 marker/template snapshot 或 schema validation tests。

### AO-EVAL-10：收敛 `DomainEvent` schema 与 TypeScript 类型

建议标题：`Align DomainEvent schema and TypeScript event model`

优先级：P2

建议标签：`type:refactor`、`area:webhook`、`area:contracts`、`priority:p2`

建议 milestone：M12 - Maintainability Cleanup

背景：

- `domain-event.schema.json` 允许 `source: webhook | reconciliation | manual`，且 event_type enum 覆盖 agent 内部事件、policy block、retry exhausted 等。
- `src/webhooks/domain-event.ts` 的 `DomainEvent.source` 类型仅允许 `"webhook"`，`DomainEventType` 只包含 webhook-normalized 的 9 类事件。

范围：

- 决定是否拆分 `WebhookDomainEvent` 与 broader `WorkflowEvent`，或拓宽当前 TS 类型。
- 让 schema、state-machine event、webhook-normalized event 的命名边界清晰。
- 避免把 webhook intake 类型误用为 reconciliation/manual event 类型。

验收标准：

- schema 与 TS 类型的差异有明确设计，不再是隐式漂移。
- reconciliation/manual event 如仍在 schema 中，代码有可表达类型。
- webhook-only 类型命名不误导。

验证：

- `npm run check`
- Type-level exports 和 domain-event tests 更新。

### AO-EVAL-11：明确 repo `routing.rules` 与 local `agent_routing` 的关系

建议标题：`Document routing.rules as reserved or implement repository-level routing`

优先级：P2

建议标签：`type:docs`、`area:config`、`area:agents`、`priority:p2`

建议 milestone：M11 - Operator Readiness 或 M12 - Maintainability Cleanup

背景：

- `repo-policy.schema.json` 允许 `routing.rules`。
- 当前运行时 agent routing 使用本地 `local-config.agent_routing.catalog/profiles`。
- 主方案、契约和实现对“仓库级 routing”与“本地 agent catalog/profile routing”的边界描述不一致。

范围：

- 若仓库级 routing 预留：在 schema / docs 中明确 MVP 不执行 `repo-policy.routing.rules`。
- 若要实现：把 repo policy routing 接入 `RoutingAgentAdapter`，并定义安全边界。
- 更新主方案、`03-internal-module-interfaces.md`、policy/config schema 文档。

验收标准：

- 用户不会误以为 `.github/agent-orchestrator.json` 的 `routing.rules` 已生效。
- 本地 `agent_routing` 的 profile/catalog 示例清晰。

验证：

- `npm run check`
- Config validation docs/examples 复核。

### AO-EVAL-12：统一 idempotency key 格式或更新契约

建议标题：`Normalize idempotency key format across lifecycle actions`

优先级：P2

建议标签：`type:cleanup`、`area:idempotency`、`area:contracts`、`priority:p2`

建议 milestone：M12 - Maintainability Cleanup

背景：

- `data-contracts.md` 建议格式为 `run_id:state:head_sha:action`。
- 当前代码使用多种稳定变体，如 `runId:merge:pull-request`、`runId:planner:plan-comment`、`runId:transition:event:state`。
- 功能上可幂等，但格式契约与实现不完全一致。

范围：

- 决定统一实现格式，或把契约改成“stable key derived from run id + action target”。
- 避免破坏已有 idempotency/reconciliation 记录。
- 与 AO-EVAL-05 的 full lifecycle action audit 一起考虑迁移。

验收标准：

- 契约和实现一致。
- key 可读、稳定、不会跨不同 action 冲突。
- replay / request hash conflict 行为不变。

验证：

- `npm run check`
- idempotent action tests 覆盖旧/新格式策略。

### AO-EVAL-13：对齐 scheduler 与 label-gating 契约

建议标题：`Align scheduler apply with label-gating contract`

优先级：P1

建议标签：`type:hardening`、`area:policy`、`area:reconciliation`、`area:scheduler`、`priority:p1`

建议 milestone：M10 - Quality Gates And Verification

关联 issue：**#40（已存在，直接跟进，无需重复录入）**

背景：

- `task-state-contracts.md` 要求 scheduler 不得 claim 带有 pause、blocked、terminal 或 human-control labels 的 issue/PR。
- 当前 `src/reconciliation/scheduler.ts` 仅依据本地 run state、lease、retry count 和 last error 决策。
- lifecycle 控制层可能仍会阻断执行，但 scheduler 可能对 GitHub labels 应阻止调度的 run 不必要地 claim ownership。

范围：

- 在 scheduler `--apply` 决策中纳入 issue/PR label 快照并跳过 blocked/human-control labels，或收窄 minimal scheduler 契约并同步文档。
- 与 AO-EVAL-06 区分：本任务聚焦 scheduler claim 决策，不替代主生命周期状态 label 同步。

验收标准：

- scheduler 行为与 `task-state-contracts.md` label-gating 语义一致。
- 带 `agent:pause`、`needs-human`、`agent:blocked` 等控制/阻断 label 的 run 不会被 `--apply` 不必要 claim。
- 文档明确最小 scheduler 与 lifecycle 控制层的职责边界。

验证：

- `npm run check`
- scheduler / reconciliation 定点测试覆盖 label-gated skip 场景。

## 建议录入顺序

已有 open issue 的条目（AO-EVAL-13 → #40）直接跟进，不重复录入。

1. AO-EVAL-04：merge gate 当前 GitHub 证据。
2. AO-EVAL-05：material GitHub write action audit（补齐 branch/commit/PR/review/merge/close 等待记录项）。
3. AO-EVAL-13：跟进已有 #40，对齐 scheduler label-gating。
4. AO-EVAL-02：Webhook path/status contract。
5. AO-EVAL-03：Webhook event coverage。
6. AO-EVAL-06：状态 label 同步（与 #41 协调边界）。
7. AO-EVAL-09：Schema/template 修复。
8. AO-EVAL-08：prompt injection enforcement。
9. AO-EVAL-07：控制命令。
10. AO-EVAL-01：主方案整体文档对齐（与 #19/#21/#23 协调边界）。
11. AO-EVAL-10、AO-EVAL-11、AO-EVAL-12：维护性收敛。

## issue 录入模板

```markdown
## 背景

来自 `docs/development-plan/评估差异任务清单.md` 的 AO-EVAL-XX。

## 范围

- ...

## 不在范围

- ...

## 验收标准

- ...

## 验证

- [ ] `npm run check`
- [ ] 相关定点测试：...

## 需要更新的文档

- ...
```
